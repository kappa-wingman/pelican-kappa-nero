{% macro title(name, no_icon, icon='home') -%}
  {%- if no_icon -%}
    {{ name }}
  {%- else -%}
    {#<i class="fa fa-{{ icon }} fa-lg"></i><span class="icon-label">{{ name }}</span>#}
    <i data-feather="{{ icon }}"></i><span class="icon-label">{{ name }}</span>
  {%- endif -%}
{%- endmacro %}
